/**
 * Telemetry configuration.
 *
 * 1. PostHog: create a free project at https://posthog.com → Project Settings →
 *    copy the Project API key (starts with "phc_") and paste it below.
 * 2. Sentry: create a free project at https://sentry.io (platform: React Native)
 *    → copy the DSN and paste it below.
 *
 * Both are safe no-ops while left empty — the app runs normally without them.
 */
export const POSTHOG_API_KEY = 'phc_r9KMPZ7jmXuSuCsm9DGKbnFJmaYK4wkrC6uvctJKHLez';
export const POSTHOG_HOST = 'https://us.i.posthog.com';
export const SENTRY_DSN = '';
